import { useState } from "react";

export default function FolderItem(props: any) {
    return (
        // @ts-ignore
        <div style={{"padding-left": `${props.width * 5}px`}}>

        </div>
    )
}